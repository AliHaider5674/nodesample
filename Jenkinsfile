@Library('my-shared-library2@main')_

jenkinsfile{
message = "hello world it is my jenkins call from another repo"
 
}

/*pipeline {

    agent {
        docker {
            image 'node'
            args '-u root'
        }
    }

    stages {
        stage('Build') {
            steps {
                script {
                    sample()
                }
            }
        }
    }
}
*/
